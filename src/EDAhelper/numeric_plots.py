def numeric_plots(df, column_names=None, ncols=5):
    """
    Takes a dataframes and plot the possible pairs of the numeric columns using Altair, 
    creating a matrix of correlation plots

    Parameters:
        df (pandas.dataframe): a pandas dataframe
        column_names (list): The column_names to be plotted.  If it is `None` (default value), 
                             it tries to plot all columns

    Returns:
        None
    """
    return


print(numeric_plots.__doc__)
